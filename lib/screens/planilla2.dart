import 'package:flutter/material.dart';
import 'report_generator_screen.dart';

class Planilla2Page extends StatelessWidget {
  const Planilla2Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ReportGeneratorScreen(),
    );
  }
}